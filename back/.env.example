# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=.railway.app,localhost,127.0.0.1

# Database (Railway will provide DATABASE_URL automatically)
USE_POSTGRES=True
DATABASE_URL=postgresql://user:password@host:port/dbname

# CORS Settings (Add your Vercel frontend URL)
CORS_ALLOWED_ORIGINS=https://your-vercel-app.vercel.app,http://localhost:8080

# Optional: Individual database settings (if not using DATABASE_URL)
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
