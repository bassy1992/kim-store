# System Architecture - Kimmy's Fragrance Store

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│  React Frontend (Vercel)                                         │
│  - React 18 + TypeScript                                         │
│  - React Router 6 (SPA)                                          │
│  - TailwindCSS 3                                                 │
│  - Radix UI Components                                           │
│  - Vite Build Tool                                               │
└────────────────────┬────────────────────────────────────────────┘
                     │ HTTPS/REST API
                     │
┌────────────────────▼────────────────────────────────────────────┐
│                      API GATEWAY LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│  Django REST Framework (Railway)                                 │
│  - Token + Session Authentication                                │
│  - CORS Configuration                                            │
│  - Rate Limiting                                                 │
│  - API Documentation (Swagger)                                   │
└────────────────────┬────────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────────┐
│                    APPLICATION LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│  Django Apps (6 Apps)                                            │
│  ┌──────────────┬──────────────┬──────────────┐                │
│  │  Products    │  Orders      │  Customers   │                │
│  │  - Catalog   │  - Cart      │  - Profiles  │                │
│  │  - Images    │  - Checkout  │  - Auth      │                │
│  │  - Categories│  - Tracking  │              │                │
│  └──────────────┴──────────────┴──────────────┘                │
│  ┌──────────────┬──────────────┬──────────────┐                │
│  │  Reviews     │  Blog        │  Content     │                │
│  │  - Ratings   │  - Posts     │  - FAQs      │                │
│  │  - Comments  │  - Articles  │  - Policies  │                │
│  │              │              │  - CMS       │                │
│  └──────────────┴──────────────┴──────────────┘                │
└────────────────────┬────────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────────┐
│                      DATA LAYER                                  │
├─────────────────────────────────────────────────────────────────┤
│  PostgreSQL Database (Railway)                                   │
│  - Products & Categories                                         │
│  - Orders & Cart Items                                           │
│  - Users & Profiles                                              │
│  - Reviews & Ratings                                             │
│  - Blog Posts                                                    │
│  - CMS Content                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow

### 1. Product Browsing
```
User → Frontend → GET /api/products/ → Django → PostgreSQL
                                      ↓
                                   Response
                                      ↓
     Frontend ← JSON (Products) ← Django
```

### 2. Add to Cart
```
User → Frontend → POST /api/cart/items/ → Django → PostgreSQL
                                         ↓
                                      Create CartItem
                                         ↓
     Frontend ← JSON (Cart) ← Django ← Save
```

### 3. Checkout
```
User → Frontend → POST /api/orders/ → Django → PostgreSQL
                                     ↓
                                  Create Order
                                     ↓
                                  Clear Cart
                                     ↓
     Frontend ← JSON (Order) ← Django
```

---

## 🗂️ Database Schema

### Core Tables

```
┌─────────────────┐
│   Categories    │
├─────────────────┤
│ id              │
│ name            │
│ slug            │
│ description     │
└────────┬────────┘
         │
         │ 1:N
         │
┌────────▼────────┐
│    Products     │
├─────────────────┤
│ id              │
│ name            │
│ slug            │
│ description     │
│ price           │
│ category_id     │◄────┐
│ product_type    │     │
│ scent_family    │     │
│ stock_quantity  │     │
│ is_featured     │     │
└────────┬────────┘     │
         │              │
         │ 1:N          │ N:1
         │              │
┌────────▼────────┐     │
│ ProductImages   │     │
├─────────────────┤     │
│ id              │     │
│ product_id      │─────┘
│ image           │
│ is_primary      │
│ order           │
└─────────────────┘

┌─────────────────┐
│     Users       │
├─────────────────┤
│ id              │
│ username        │
│ email           │
│ password        │
└────────┬────────┘
         │
         │ 1:1
         │
┌────────▼────────┐
│CustomerProfiles │
├─────────────────┤
│ id              │
│ user_id         │
│ phone           │
│ address         │
└────────┬────────┘
         │
         │ 1:N
         │
┌────────▼────────┐
│     Carts       │
├─────────────────┤
│ id              │
│ user_id         │
│ session_key     │
└────────┬────────┘
         │
         │ 1:N
         │
┌────────▼────────┐
│   CartItems     │
├─────────────────┤
│ id              │
│ cart_id         │
│ product_id      │
│ quantity        │
│ size            │
└─────────────────┘

┌─────────────────┐
│     Orders      │
├─────────────────┤
│ id              │
│ order_number    │
│ user_id         │
│ email           │
│ full_name       │
│ address         │
│ phone           │
│ status          │
│ total_amount    │
└────────┬────────┘
         │
         │ 1:N
         │
┌────────▼────────┐
│   OrderItems    │
├─────────────────┤
│ id              │
│ order_id        │
│ product_id      │
│ product_name    │
│ product_price   │
│ quantity        │
│ size            │
└─────────────────┘

┌─────────────────┐
│    Reviews      │
├─────────────────┤
│ id              │
│ product_id      │
│ user_id         │
│ reviewer_name   │
│ rating          │
│ comment         │
└─────────────────┘
```

---

## 🔐 Security Architecture

### Authentication Flow
```
1. User Login
   ↓
2. Django validates credentials
   ↓
3. Generate Token + Session
   ↓
4. Return Token to Frontend
   ↓
5. Frontend stores Token
   ↓
6. Include Token in API requests
   ↓
7. Django validates Token
   ↓
8. Process request
```

### Security Layers
```
┌─────────────────────────────────────┐
│  HTTPS/TLS Encryption               │
├─────────────────────────────────────┤
│  CORS Protection                    │
├─────────────────────────────────────┤
│  CSRF Protection                    │
├─────────────────────────────────────┤
│  Token Authentication               │
├─────────────────────────────────────┤
│  Permission Classes                 │
├─────────────────────────────────────┤
│  Django ORM (SQL Injection)         │
├─────────────────────────────────────┤
│  Input Validation                   │
└─────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Development
```
┌──────────────┐     ┌──────────────┐
│   Frontend   │     │   Backend    │
│ localhost:   │────▶│ localhost:   │
│   5173       │     │   8000       │
└──────────────┘     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │   SQLite     │
                     │   db.sqlite3 │
                     └──────────────┘
```

### Production
```
┌──────────────┐     ┌──────────────┐
│   Vercel     │     │   Railway    │
│   Frontend   │────▶│   Backend    │
│   (CDN)      │     │   (API)      │
└──────────────┘     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │  PostgreSQL  │
                     │   Database   │
                     └──────────────┘
```

---

## 📡 API Architecture

### RESTful Endpoints Structure
```
/api/
├── products/
│   ├── GET    /              # List products
│   ├── GET    /{id}/         # Product detail
│   ├── POST   /              # Create (admin)
│   ├── PUT    /{id}/         # Update (admin)
│   └── DELETE /{id}/         # Delete (admin)
│
├── categories/
│   ├── GET    /              # List categories
│   └── GET    /{id}/         # Category detail
│
├── cart/
│   ├── GET    /              # Get cart
│   ├── POST   /items/        # Add item
│   ├── PUT    /items/{id}/   # Update item
│   ├── DELETE /items/{id}/   # Remove item
│   └── DELETE /clear/        # Clear cart
│
├── orders/
│   ├── GET    /              # List orders
│   ├── GET    /{id}/         # Order detail
│   └── POST   /              # Create order
│
├── reviews/
│   ├── GET    /              # List reviews
│   ├── GET    /{id}/         # Review detail
│   └── POST   /              # Create review
│
├── blog/
│   ├── GET    /              # List posts
│   └── GET    /{slug}/       # Post detail
│
├── faqs/
│   └── GET    /              # List FAQs
│
├── testimonials/
│   ├── GET    /              # List all
│   └── GET    /featured/     # Featured only
│
├── gift-cards/
│   └── GET    /              # List gift cards
│
├── dupes/
│   ├── GET    /              # List dupes
│   ├── GET    /brands/       # List brands
│   └── GET    /{slug}/       # Dupe detail
│
├── contact/
│   └── POST   /              # Submit form
│
└── newsletter/
    ├── POST   /subscribe/    # Subscribe
    └── POST   /unsubscribe/  # Unsubscribe
```

---

## 🔄 Request/Response Cycle

### Typical API Request
```
1. Frontend makes request
   ↓
2. CORS middleware validates origin
   ↓
3. Authentication middleware validates token
   ↓
4. URL router matches endpoint
   ↓
5. View function processes request
   ↓
6. Serializer validates/transforms data
   ↓
7. ORM queries database
   ↓
8. Serializer formats response
   ↓
9. Response sent to frontend
```

---

## 📦 Component Architecture

### Frontend Components
```
App.tsx
├── Router
│   ├── Index (Home)
│   ├── Shop
│   │   └── ProductCard
│   ├── ProductDetail
│   │   ├── ProductImages
│   │   ├── ProductInfo
│   │   └── Reviews
│   ├── Cart
│   │   └── CartItem
│   ├── Checkout
│   │   └── CheckoutForm
│   ├── About
│   ├── Contact
│   │   └── ContactForm
│   ├── FAQs
│   ├── Blog
│   │   └── BlogPost
│   └── NotFound
└── UI Components
    ├── Button
    ├── Input
    ├── Card
    ├── Dialog
    └── ...
```

### Backend Apps
```
Django Project
├── Products App
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
├── Orders App
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
├── Customers App
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
├── Reviews App
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
├── Blog App
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   └── urls.py
└── Content App
    ├── models.py
    ├── views.py
    ├── serializers.py
    └── urls.py
```

---

## 🎯 Performance Considerations

### Caching Strategy
```
┌─────────────────┐
│  Browser Cache  │  (Static assets)
├─────────────────┤
│  CDN Cache      │  (Frontend on Vercel)
├─────────────────┤
│  API Response   │  (Django cache framework)
├─────────────────┤
│  Database       │  (PostgreSQL query cache)
└─────────────────┘
```

### Optimization Points
- ✅ Database indexing on slugs and foreign keys
- ✅ Pagination (20 items per page)
- ✅ select_related() for foreign keys
- ✅ prefetch_related() for reverse relations
- ✅ WhiteNoise for static file compression
- ✅ CDN for frontend assets (Vercel)

---

## 📈 Scalability

### Horizontal Scaling
```
Load Balancer
     │
     ├─── Django Instance 1
     ├─── Django Instance 2
     └─── Django Instance 3
              │
         PostgreSQL
         (with read replicas)
```

### Future Enhancements
- Redis for caching
- Celery for background tasks
- S3 for media storage
- Elasticsearch for search
- Message queue for async processing

---

**Architecture designed for scalability, security, and maintainability! 🏗️**
