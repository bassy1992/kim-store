[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd back && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd back && python manage.py collectstatic --noinput"]

[start]
cmd = "cd back && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
